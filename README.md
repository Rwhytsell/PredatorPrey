PredatorPrey


***---Setup and running---***

Make sure python 3.6 is installed.

Using pip, install the pygame module - pip install pygame

In the command line go to the directory of the extracted PredatorPrey application

Run the application script - python application.py 
